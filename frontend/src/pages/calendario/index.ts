/* Barrel de la vista Calendario. Solo se expone el componente de pagina que
 * consume el router; el resto de piezas son internas de la vista. */
export { default as CalendarDashboard } from "./CalendarDashboard";
